<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <style>

        header{
            background-color: hotpink;
            height: 100px;
            width: 100%;
        }

       footer{
           background-color: hotpink;
           height: 100px;
           width: 100%;

       }

        .all-articles{
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
        }

        h1{
            color: hotpink;
            text-align: center;
        }
        article{
            width: 40%;

        }
        img {
            margin: 0 auto ;
            width: 100%;
            height: 300px;
            overflow: hidden;
        }

        p{
            text-align: justify;
        }



    </style>
</head>
<body>
<header></header>

<main>
    <div class="all-articles">
        {#je creer une condition (if)qui permet/ou non a l'utilisateur selon
        son âge d'acceder au site;
        via un paramètre d’une requête GET
        en utilisant  la méthode get()#}
        {% if app.request.query.get("age") >= 18 %}
            {% for article in articles %}
                <article>
                    <h1>{{ article.titre }}</h1>

                    <img src="{{ article.img }}" alt="">

                    <p>{{ article.contenu }}</p>
                </article>
            {% endfor %}
            {% else %}
            <p>accès refusé!</p>
        {% endif %}

    </div>
</main>
<footer>

    <a href="{{ path('contact') }}">contact!</a>

</footer>

</body>
</html>